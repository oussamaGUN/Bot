# Use the official ollama image as base
FROM ollama/ollama:latest
EXPOSE 11434
# (Optional) Set working directory


# (Optional) Copy your app files into the container
# COPY . /app

# (Optional) Set default command (if you want to start ollama serve automatically)
# CMD ["ollama", "serve"]

# If you just want the image as is, you can leave it here.

